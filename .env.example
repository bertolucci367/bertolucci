# Copy this file to .env.local and fill in real values.
# Next.js will load .env.local automatically in dev and build.

# Absolute URL to your GraphCMS (Hygraph) content API endpoint
# Example (region may vary): https://api-sa-east-1.hygraph.com/v2/<YOUR_PROJECT_ID>/master
# Older projects may use graphcms.com domain; both are fine as long as it is absolute (starts with https://)
GRAPHCMS_API=https://api-sa-east-1.graphcms.com/v2/YOUR_PROJECT_ID/master

# Outbound email (used by components/libs/sendEmail.js)
SENDGRID_API=https://api.sendgrid.com/v3/mail/send
SENDGRID_KEY=REPLACE_WITH_YOUR_SENDGRID_API_KEY
MAIL_TO=contato@example.com
MAIL_FROM=site@example.com

# Public site URL used in email templates
SITE_URL=http://localhost:3000
